interface:
  display_name: "Mule Build"
  short_description: "Validate, package, run, version, and tag Mule applications"
  default_prompt: "Use $mule-build to validate and package this Mule application without performing release actions."
